[1mdiff --git a/radio-orchestrator/main.py b/radio-orchestrator/main.py[m
[1mindex 25c5793..461bf61 100644[m
[1m--- a/radio-orchestrator/main.py[m
[1m+++ b/radio-orchestrator/main.py[m
[36m@@ -69,6 +69,9 @@[m [mdef clean_for_tts(text: str) -> str:[m
     text = re.sub(r"\*(.+?)\*", r"\1", text)[m
     text = re.sub(r"__(.+?)__", r"\1", text)[m
     text = re.sub(r"(?<!\w)_(.+?)_(?!\w)", r"\1", text)[m
[32m+[m[32m    # 水平線・区切り線(---, ***, ___ が3個以上、行全体を占める行)を除去。[m
[32m+[m[32m    # 除去せずTTSに渡すと、記号の連続がノイズ的な音として発音されることがある[m
[32m+[m[32m    text = re.sub(r"^\s*([-*_])\1{2,}\s*$\n?", "", text, flags=re.MULTILINE)[m
     text = re.sub(r"^#{1,6}\s*.*$\n?", "", text, flags=re.MULTILINE)[m
     text = re.sub(r"^[\-\*・]\s+", "", text, flags=re.MULTILINE)[m
     text = re.sub(r"`{1,3}(.+?)`{1,3}", r"\1", text, flags=re.DOTALL)[m
[36m@@ -132,6 +135,7 @@[m [mdef run_pipeline(request):[m
         return {"status": "error", "failed_step": "summarize", "steps": steps}, 502[m
 [m
     script_text = summarize_result.get("script_text", "")[m
[32m+[m[32m    script_text_tts = summarize_result.get("script_text_tts", script_text)[m
     if summarize_result.get("warning"):[m
         notify_discord(f"step=summarize warning={summarize_result.get('warning')}", "WARN")[m
     notify_discord([m
[36m@@ -147,7 +151,7 @@[m [mdef run_pipeline(request):[m
         return result[m
 [m
     notify_discord("step=tts status=start", "INFO")[m
[31m-    tts_text = clean_for_tts(script_text)[m
[32m+[m[32m    tts_text = clean_for_tts(script_text_tts)[m
     tts_result = call_service(TTS_SERVICE_URL, {"text": tts_text, "filename": audio_filename})[m
     steps["tts"] = {[m
         "status": tts_result.get("status"),[m
